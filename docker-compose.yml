
version: '3.8'

services:
  mlt-lms:
    build: .
    container_name: mlt_training_system
    ports:
      - "8080:80"
    environment:
      - API_KEY=${API_KEY:-your_api_key_here}
    restart: always
